import { FilterFromListPipe } from './filter-from-list.pipe';

describe('FilterFromListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFromListPipe();
    expect(pipe).toBeTruthy();
  });
});
